# MentalCarePilot Web — Environment
# Copy to .env and adjust for local dev
#
# Leave VITE_API_BASE_URL unset in dev to use Vite proxy (/api → localhost:8000)
# Set to full API URL (e.g. http://localhost:8000) for direct requests
# VITE_API_BASE_URL=http://localhost:8000
#
# Auth redirect URIs (web vs native). In Capacitor, set to capacitor://localhost/auth/callback
# VITE_AUTH_REDIRECT_URI=http://localhost:5173/auth/callback
# VITE_AUTH_POST_LOGOUT_REDIRECT_URI=http://localhost:5173
#
# Azure B2C (optional until B2C is wired)
# VITE_B2C_AUTHORITY=https://<tenant>.b2clogin.com/<tenant>.onmicrosoft.com/<policy>
# VITE_B2C_CLIENT_ID=<client-id>
# VITE_B2C_SCOPES=openid profile
